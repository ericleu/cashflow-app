{
  "name": "cashflow-apps-script",
  "private": true,
  "scripts": {
    "build": "tsc && cp appsscript.json dist/",
    "push": "npm run build && npx clasp push --force",
    "deploy": "npm run build && npx clasp push --force"
  },
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^1.0.83",
    "typescript": "^5.4.0"
  }
}
